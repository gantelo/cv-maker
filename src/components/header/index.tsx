import './styles.css';

import { InputClean } from 'src/components';

const Header = () => {
  return (
    <div className="headerContainer">
      <InputClean />
    </div>
  );
};

export default Header;
