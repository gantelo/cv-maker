import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
  sectionTitle: {
    fontSize: 10,
    marginTop: 22,
    marginBottom: 8,
    fontFamily: 'Roboto',
    fontWeight: 'medium',
  },
  detailsItem: {
    fontSize: 9,
    marginTop: 2,
    fontFamily: 'Roboto',
  },
});
