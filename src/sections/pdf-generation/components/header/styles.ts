import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
  title: {
    fontSize: 8,
    marginTop: 14,
    fontFamily: 'Roboto',
  },
  subHeaderContainer: {
    justifyContent: 'center',
    display: 'flex',
    flexDirection: 'row',
  },
  header: {
    fontSize: 17,
    marginTop: 30,
    textAlign: 'center',
    fontFamily: 'Roboto',
    fontWeight: 'medium',
  },
});
