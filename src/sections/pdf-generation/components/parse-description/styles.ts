import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
  line: {
    fontSize: 9,
    marginTop: 3,
    fontFamily: 'Roboto',
  },
  marginLeft: {
    marginLeft: 8,
  },
  container: {
    marginTop: 5,
  },
});
