import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
  sectionTitle: {
    fontSize: 10,
    marginTop: 22,
    marginBottom: 8,
    fontFamily: 'Roboto',
    fontWeight: 'medium',
  },
  profileSummary: {
    fontSize: 9,
    marginTop: 2,
    marginLeft: 10,
    fontFamily: 'Roboto',
  },
});
